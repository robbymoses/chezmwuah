# Shared chezmoi template fragments

Put reusable template fragments here. Files in this directory are templates
but are not installed by themselves. Reference a fragment from a target
`*.tmpl` file with `{{ template "name" . }}`.
