# Ghostty

`config.ghostty` maps to `~/.config/ghostty/config.ghostty`. It contains the
terminal preferences and selects the local `Eldritch` theme. The matching theme
definition lives at `themes/Eldritch`; keep managed Ghostty themes in that
directory and select them with `theme = <name>` in `config.ghostty`.
